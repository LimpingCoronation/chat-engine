# Chat Engine
Simple lightweight chat system based on fastapi and websocket stack

### Setup
1. Create .env file in root folder
3. Run migrations by following command:
 - alembic upgrade head
4. To downgrade migrations
 - alembic downgrade -1